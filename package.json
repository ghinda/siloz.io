{
  "name": "siloz.io",
  "private": true,
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "clean": "mkdir -p build && rm -rf build/ && mkdir -p build",
    "dev": "npm run clean && grunt server",
    "build": "npm run clean && cp -a static/** build/ && grunt",
    "deploy": "npm run build && grunt deploy",
    "test": "grunt test"
  },
  "author": "",
  "license": "AGPL",
  "dependencies": {},
  "devDependencies": {
    "babel-preset-es2015": "^6.16.0",
    "babelify": "^7.3.0",
    "codemirror": "^5.20.2",
    "connect-livereload": "^0.6.0",
    "durruti": "^0.0.16",
    "grunt": "^1.0.1",
    "grunt-assemble": "^0.5.0",
    "grunt-browserify": "^5.0.0",
    "grunt-build-control": "^0.7.1",
    "grunt-contrib-connect": "^1.0.2",
    "grunt-contrib-stylus": "^1.2.0",
    "grunt-contrib-uglify": "^2.0.0",
    "grunt-contrib-watch": "^1.0.0",
    "grunt-saucelabs": "^9.0.0",
    "grunt-standard": "^2.0.0",
    "jotted": "^1.4.5",
    "lz-string": "^1.4.4",
    "matchdep": "^1.0.0",
    "tape": "^4.6.2"
  }
}
